var height = Number(prompt("Enter the rectangle's height"));
var width = Number(prompt("Enter the rectangles's width"));





document.write(`Area of rectangle = Height*Width = ${height}*${width} = ${height*width}`);