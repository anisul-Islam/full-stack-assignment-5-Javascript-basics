function rectangle(){
    let length= Number(prompt("Enter Rectangle length : "));
    let width= Number(prompt("Enter Rectangle width : "));
    let area= width*length;
    alert(`Rectangle area= ${area}`);
}

