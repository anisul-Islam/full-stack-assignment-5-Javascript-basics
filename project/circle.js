function calculateCircle() {
  let radius = parseFloat(prompt("Enter the radius of the circle:"));
  let areaOfCircle = 3.1416 * radius * radius;
  alert("The area of the circle is: " + areaOfCircle);
}
