const base = Number(prompt("Enter a triangle base number:"));
const height = Number(prompt("Enter a triangle height number:"));
const triangle = 0.5 + base + height;
console.log("Area of triangle = " + triangle);
