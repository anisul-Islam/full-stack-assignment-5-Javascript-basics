const length = Number(prompt("Enter a rectangle length number:"));
const width = Number(prompt("Enter a rectangle width number:"));
const rectangle = length * width;
console.log("Area of rectangle = " + rectangle);
