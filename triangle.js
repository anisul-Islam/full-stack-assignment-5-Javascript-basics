const base=Number(prompt("Enter The Base of Triangle"));
const height=Number(prompt("Enter The Height of Triangle"));
const area1=0.5*base*height;
document.getElementById('triangle').innerHTML=(`Area of Triangle = ${area1}`);
