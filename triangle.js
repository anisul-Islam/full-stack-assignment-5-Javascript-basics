function triangle() {

    let base = Number(prompt("Enter the triangle base : "));
    let height = Number(prompt("Enter the triangle height : "));

    let area = 0.5 * base * height;

    alert(`Area of Triangle is = ${area}`);
}