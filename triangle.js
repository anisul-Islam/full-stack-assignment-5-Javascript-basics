

var height = Number(prompt("Enter the triangle's height"));
var base = Number(prompt("Enter the triangle's base"));





console.log(`Area of triangle = 1/2(Base*Height) = 1/2*${base}*${height} = ${1/2*base*height}`);