let base = parseInt(prompt('Enter triangle base ='));
let height = parseInt(prompt('Enter triangle height ='))
let triangleArea = 0.5 * base * height;
document.write(`Triangle area=${triangleArea} <br>`)